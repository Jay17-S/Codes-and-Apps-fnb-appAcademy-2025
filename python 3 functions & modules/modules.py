# Modules are python files with .py extensions. which implements a set of functions
# Modules are imported ffrom other modules using "import mod"
# to create a module we first create a new python file
# modules provide reusability of code

import math_operations

print(math_operations.add(5, 2))

print(math_operations.subtract(5, 2))